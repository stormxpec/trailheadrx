# Program records (local snapshots only)

Manufacturer DTC, copay card, patient assistance, and discount-card records are extracted into `manifest.yaml` with a source URL and `verified_on` date. Page snapshots saved here are ignored by git.

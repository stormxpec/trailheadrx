# Policy corpus (local only)

Download each document listed in `manifest.yaml` into this folder by hand and fill in its `file`, `url`, `version`, `effective_date`, and `downloaded_on`. The PDFs are ignored by git; only this README and the manifest are committed.
